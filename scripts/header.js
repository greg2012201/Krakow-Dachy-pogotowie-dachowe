const header = document.querySelector('.header');
const mask = document.querySelector('.header--mask');
console.log(mask);
header.style.bacgroundColor = 'black'

console.log(header.offsetHeight);
console.log(mask.offsetHeight);